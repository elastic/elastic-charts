---
- job:
    name: elastic+elastic-charts+pr-vrt
    display-name: 'elastic / elastic-charts # pull-request VRTs'
    description: Visual Regression Testing of elastic-charts pull requests.
    builders:
      - shell: |-
          #!/usr/local/bin/runbld

          set -euo pipefail

          ./.ci/scripts/vrts.sh
